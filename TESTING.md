# Testing Guide — /Users/hendrikk/Developer/ria/Ruuter

> Generated by risttee on 2026-07-07T10:27:02.726862Z
> Profile schema: 1.0

## Quick Start

1. Copy `.env.example` to `.env` and fill in values
2. Start the service:

```bash
   docker compose -f docker-compose.yml up -d
```

3. Install Playwright dependencies:
```bash
   npm ci
   npx playwright install --with-deps
```
4. Run smoke tests:
```bash
   npx playwright test
```

## Stack

| Field | Value |
|-------|-------|
| Stack | Java / Spring Boot / Gradle |
| Port | 8080 |
| Health endpoint | /actuator/health |

## Smoke Test Coverage

10 tests generated from
55 detected surfaces.

| Method | Path | Reason |
|--------|------|--------|

| GET | `/healthz` | health check |

| GET | `/common/skip` | simple GET endpoint |

| GET | `/common/sleep` | simple GET endpoint |

| GET | `/guards/noguard/mock-response` | simple GET endpoint |

| GET | `/guards/ok/mock-response` | simple GET endpoint |

| GET | `/steps/get/get` | simple GET endpoint |

| POST | `/test-text` | simple POST endpoint |

| POST | `/dynamic-body` | simple POST endpoint |

| POST | `/steps/post/post-with-plaintext-value` | simple POST endpoint |

| GET | `/api` | simple GET endpoint |


## Coverage Gaps

- **Guarded endpoints**: 3 surfaces have a `.guard`
  file — authentication setup is required before these tests can pass.
- **Untested surfaces**: 45
  surfaces were detected but not included in the smoke set.
  See `.kratest/ASSUMPTIONS.md` for suggested next targets.

## CI

A GitHub Actions workflow has been generated at
`.github/workflows/playwright.yml`.
It starts the service, waits for the health endpoint, and runs the
full smoke suite on every push and pull request.
