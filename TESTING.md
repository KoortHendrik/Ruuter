# Testing Guide — /Users/hendrikk/Developer/ria/Ruuter

> Generated by risttee on 2026-07-23T07:09:15.853729Z
> Profile schema: 1.2

## Quick Start

1. Copy `.env.example` to `.env` and fill in values
2. Start the service:

```bash
   docker compose -f docker-compose.yml up -d
```

3. Install Playwright dependencies:
```bash
   npm ci
   npx playwright install --with-deps
```
4. Run smoke tests:
```bash
   npx playwright test
```

## Stack

| Field | Value |
|-------|-------|
| Stack | Java / Spring Boot / Gradle |
| Port | 8080 |
| Health endpoint | /actuator/health |

## Smoke Test Coverage

9 tests generated from
46 detected surfaces.

| Method | Path | Reason |
|--------|------|--------|

| GET | `/healthz` | primary health check |

| GET | `/common/skip` | simple GET with no inputs |

| GET | `/common/sleep` | simple GET with no inputs |

| GET | `/steps/get/get` | basic GET endpoint |

| POST | `/test-text` | simplest POST without inputs |

| POST | `/dynamic-body` | POST with single input, low complexity |

| GET | `/api` | generic GET endpoint, useful for basic availability |

| POST | `/**/text/*` | wildcard POST, tests routing and request handling |

| GET | `/steps/assign/assign` | simple GET that exercises step assignment logic |


## Coverage Gaps

- **Guarded endpoints**: 3 surfaces have a `.guard`
  file — authentication setup is required before these tests can pass.
- **Untested surfaces**: 37
  surfaces were detected but not included in the smoke set.
  See `.kratest/ASSUMPTIONS.md` for suggested next targets.

## CI

A GitHub Actions workflow has been generated at
`.github/workflows/playwright.yml`.
It starts the service, waits for the health endpoint, and runs the
full smoke suite on every push and pull request.
